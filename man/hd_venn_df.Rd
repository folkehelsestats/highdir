% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-venn.R
\name{hd_venn_df}
\alias{hd_venn_df}
\title{Create a Venn Diagram Directly from a Data Frame}
\usage{
hd_venn_df(
  df,
  output = c("spec", "geom"),
  series_name = "Venn Diagram",
  label_font_size = "14px",
  value_suffix = "",
  use_names = FALSE,
  show_legend = FALSE
)
}
\arguments{
\item{df}{A data frame with columns \code{id}, \code{name}, \code{value},
\code{type}.  See \code{\link[=venn_df_to_list]{venn_df_to_list()}} for the full format description.}

\item{output}{Character. \code{"spec"} (default) returns an
\code{hd_spec_venn}; \code{"geom"} returns an \code{hd_geom} layer for use with \code{+}.}

\item{series_name}{Character. Passed to \code{\link[=hd_geom_venn]{hd_geom_venn()}} when
\code{output = "geom"}.  Default \code{"Venn Diagram"}.}

\item{label_font_size}{Character. Passed to \code{\link[=hd_geom_venn]{hd_geom_venn()}} when
\code{output = "geom"}.  Default \code{"14px"}.}

\item{value_suffix}{Character. Passed to \code{\link[=hd_geom_venn]{hd_geom_venn()}}. Suffix
appended to value labels (e.g. \code{"\%"}).  Default \code{""}.}

\item{use_names}{Logical. Passed to \code{\link[=hd_geom_venn]{hd_geom_venn()}}. Use
human-readable names instead of ids in labels.  Default \code{FALSE}.}

\item{show_legend}{Logical. Passed to \code{\link[=hd_geom_venn]{hd_geom_venn()}}. Show a
legend beneath the diagram.  Default \code{FALSE}.}
}
\value{
An \code{hd_spec_venn} object when \code{output = "spec"}, or an \code{hd_geom}
object when \code{output = "geom"}.
}
\description{
A single-call convenience wrapper that accepts the same tidy data frame as
\code{\link[=venn_df_to_list]{venn_df_to_list()}} and returns either an \code{hd_spec_venn} object (for the
declarative API) or an \code{hd_geom} layer (for the composable \code{+} API),
depending on the \code{output} argument.
}
\details{
This means you never need to call \code{\link[=venn_df_to_list]{venn_df_to_list()}}, \code{\link[=hd_spec_venn]{hd_spec_venn()}},
or \code{\link[=hd_geom_venn]{hd_geom_venn()}} directly when starting from a data frame.
}
\section{Which output to use}{

\describe{
\item{\code{output = "spec"} (default)}{Returns an \code{hd_spec_venn} object.
Pass to \code{\link[=hd_make]{hd_make()}} just like any other spec.  Best for reporting
pipelines and scripts that separate data from presentation.}
\item{\code{output = "geom"}}{Returns an \code{hd_geom} layer.  Add to an \code{\link[=hd]{hd()}}
object with \code{+}.  Best for interactive exploration and inline charts.}
}
}

\section{Declarative API (output = "spec")}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{df <- data.frame(
  type  = c("set", "set", "intersect"),
  id    = c("A",   "B",   "A,B"),
  name  = c("Oslo", "Bergen", "Both"),
  value = c(120, 95, 40)
)

spec_v <- hd_venn_df(df)
hd_make(spec_v, "venn", hd_opts(title = "City overlap"))
}\if{html}{\out{</div>}}
}

\section{Composable API (output = "geom")}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{hd(backend = "dynamic") +
  hd_venn_df(df, output = "geom") +
  hd_opts(title = "City overlap")
}\if{html}{\out{</div>}}
}

\examples{
\donttest{
df <- data.frame(
  type  = c("set", "set", "intersect"),
  id    = c("A",   "B",   "A,B"),
  name  = c("Oslo", "Bergen", "Both"),
  value = c(120, 95, 40)
)

# Declarative API
spec_v <- hd_venn_df(df)
hd_make(spec_v, "venn", hd_opts(title = "City overlap"))

# Composable API
hd(backend = "dynamic") +
  hd_venn_df(df, output = "geom") +
  hd_opts(title = "City overlap")

# ggplot2 backend
hd(backend = "static") +
  hd_venn_df(df, output = "geom") +
  hd_opts(title = "City overlap")
}

}
\seealso{
\code{\link[=venn_df_to_list]{venn_df_to_list()}}, \code{\link[=hd_spec_venn]{hd_spec_venn()}}, \code{\link[=hd_geom_venn]{hd_geom_venn()}},
\code{\link[=hd_make]{hd_make()}}, \code{\link[=hd]{hd()}}
}
