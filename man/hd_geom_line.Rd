% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-line.R
\name{hd_geom_line}
\alias{hd_geom_line}
\title{Line Geometry Layer for hd Objects}
\usage{
hd_geom_line(smooth = TRUE, dot_size = 4, line_symbols = NULL, ...)
}
\arguments{
\item{smooth}{Logical. TRUE = spline curves, FALSE = straight segments. Both backends.}

\item{dot_size}{Numeric. Marker radius in pixels. Both backends.}

\item{line_symbols}{Character vector. Highcharter only. Per-group marker shapes:
"circle", "square", "diamond", "triangle", "triangle-down".}

\item{...}{Geometry-specific arguments forwarded to \code{\link[=hd_make]{hd_make()}}.}
}
\value{
An S3 object of class \code{"hd_geom"} for use with \code{+.hd}.
}
\description{
\code{hd_geom_line()} creates a line geometry layer that is added to an \code{\link[=hd]{hd()}}
object via \code{+}.  The layer records the geometry type and any geometry-specific arguments;
rendering only happens when the \code{hd} object is printed.
}
\examples{
# Single series - no group column
spec_line1 <- hd_spec(alco1,
    x    = "year",
    y    = "adj_mean"
)

opts_line <- hd_opts(
    title = "Alcohol consumption over time",
    subtitle = "Source: Norwegian Directorate of Health",
    ylim = c(0, 50),
    ylab = "Litres per capita"
)


# Straight segments
hd_make(spec_line1, "line", opts_line, smooth = FALSE)

# Composite example with multiple geoms and custom line symbols
hd(alco2, x = "year", y = "adj_mean", group = "kjonn", backend = "static") +
  hd_geom_line(smooth = TRUE, dot_size = 3) +
  hd_opts(title = "Alcohol consumption over time by kjonn",
  subtitle = "Source: Norwegian Directorate of Health")

}
