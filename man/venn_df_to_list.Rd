% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-venn.R
\name{venn_df_to_list}
\alias{venn_df_to_list}
\title{Convert a Data Frame to a Venn Set List}
\usage{
venn_df_to_list(df)
}
\arguments{
\item{df}{A data frame with columns \code{id}, \code{name}, \code{value}, \code{type}.
Can be a plain \code{data.frame}, \code{data.table}, or \code{tibble}.}
}
\value{
A list of set entries suitable for \code{\link[=hd_geom_venn]{hd_geom_venn()}},
\code{\link[=hd_spec_venn]{hd_spec_venn()}}, and \code{\link[=hd_venn_df]{hd_venn_df()}}.
}
\description{
Converts a tidy data frame with columns \code{id}, \code{name}, \code{value}, and \code{type}
into the nested list format required by \code{\link[=hd_geom_venn]{hd_geom_venn()}} and
\code{\link[=hd_spec_venn]{hd_spec_venn()}}.  This is the recommended way to build venn data from
a spreadsheet, CSV, or database query without calling \code{\link[=hd_venn_set]{hd_venn_set()}} and
\code{\link[=hd_venn_intersect]{hd_venn_intersect()}} row by row.
}
\section{Data frame format}{

The input data frame must have these four columns:
\describe{
\item{\code{id}}{Character. For \code{type = "set"}: a single identifier such as
\code{"A"}.  For \code{type = "intersect"}: a comma-separated list of the set ids
that overlap, such as \code{"A,B"} or \code{"A,B,C"}.  Spaces around commas are
trimmed automatically.}
\item{\code{name}}{Character. Human-readable label shown in the diagram.
For intersections this is optional — supply \code{NA} or \code{""} to omit it.}
\item{\code{value}}{Numeric. Area or size of this region.}
\item{\code{type}}{Character. Either \code{"set"} (a single circle) or
\code{"intersect"} (an overlap region between two or more circles).}
}
}

\section{Relationship to hd_venn_set / hd_venn_intersect}{

Each row is converted by the corresponding constructor:
\itemize{
\item \code{type = "set"}      calls \link{hd_venn_set(id, name, value)}
\item \code{type = "intersect"} calls \link{hd_venn_intersect(ids, value, name)}
where \code{ids} is the comma-split vector from \code{id}.
}
The result is identical to building the list by hand with those functions.
}

\examples{
venn_df <- data.frame(
  type  = c("set", "set", "intersect"),
  id    = c("A",   "B",   "A,B"),
  name  = c("Oslo", "Bergen", "Both"),
  value = c(120, 95, 40)
)

sets <- venn_df_to_list(venn_df)
# Equivalent to:
# list(
#   hd_venn_set("A", "Oslo",   120),
#   hd_venn_set("B", "Bergen", 95),
#   hd_venn_intersect(c("A", "B"), 40, name = "Both")
# )

}
\seealso{
\code{\link[=hd_venn_df]{hd_venn_df()}}, \code{\link[=hd_spec_venn]{hd_spec_venn()}}, \code{\link[=hd_geom_venn]{hd_geom_venn()}}
}
