% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-venn.R
\name{hd_geom_venn}
\alias{hd_geom_venn}
\title{Venn / Euler Diagram Layer for hd Objects}
\usage{
hd_geom_venn(
  sets,
  series_name = "Venn Diagram",
  label_font_size = "14px",
  value_suffix = "",
  ...
)
}
\arguments{
\item{sets}{A list of set entries.  See \strong{Data format} above.
Use \code{\link[=hd_venn_set]{hd_venn_set()}} and \code{\link[=hd_venn_intersect]{hd_venn_intersect()}} to build entries.}

\item{series_name}{Character. Series label shown in the Highcharts
chart.  Default \code{"Venn Diagram"}.  Highcharter only.}

\item{label_font_size}{Character. CSS font-size for set labels.
Default \code{"14px"}.  Highcharter only.}

\item{...}{Additional arguments forwarded to \code{\link[=hd_make]{hd_make()}}.}
}
\value{
An S3 object of class \code{"hd_geom"} for use with \code{+.hd}.
}
\description{
Creates a Venn or Euler diagram layer that is added to an \code{\link[=hd]{hd()}} object
via \code{+}.  The diagram is described by a list of set entries supplied via
the \code{sets} argument.
}
\section{Data format}{

\code{sets} is a list of named lists.  Each element describes either a single
set or an intersection between sets:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(
  # Single sets - must have name and value
  list(sets = list("A"), name = "Animals",   value = 5),
  list(sets = list("B"), name = "Four legs", value = 4),
  list(sets = list("C"), name = "Mineral",   value = 2),
  # Intersections - sets contains two or more names, value is overlap size
  list(sets = list("A", "B"), value = 3)
)
}\if{html}{\out{</div>}}

Use \code{\link[=hd_venn_set]{hd_venn_set()}} and \code{\link[=hd_venn_intersect]{hd_venn_intersect()}} to build entries without
typing the nested list structure by hand.
}

\section{Extending the set list}{

Because \code{sets} is a plain R list, it can be built and extended
programmatically with \code{c()} or \code{append()}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{base <- list(
  hd_venn_set("A", "Animals",   5),
  hd_venn_set("B", "Four legs", 4)
)
extended <- c(base, list(hd_venn_intersect(c("A","B"), 3)))
hd(backend = "highcharter") +
  hd_geom_venn(sets = extended)
}\if{html}{\out{</div>}}
}

\section{Backend differences}{

\describe{
\item{highcharter}{Uses Highcharts' native \code{"venn"} series type.
All features (labels, tooltips, interactivity) work out of the box.
\code{series_name} and \code{label_font_size} are highcharter-only options.}
\item{ggplot2}{Requires the \pkg{eulerr} package (preferred) or
\pkg{ggVennDiagram} in \code{Suggests}.  Install with
\code{install.packages("eulerr")}.}
}
}

\examples{
# Build the set list with helper constructors
my_sets <- list(
  hd_venn_set("A", "Animals",    value = 5),
  hd_venn_set("B", "Four legs",  value = 4),
  hd_venn_set("C", "Mineral",    value = 2),
  hd_venn_intersect(c("A", "B"), value = 3)
)

# Highcharter (interactive)
hd(backend = "highcharter") +
  hd_geom_venn(sets = my_sets) +
  hd_opts(title = "Animals and Minerals")

# ggplot2 (static) - requires eulerr or ggVennDiagram
hd(backend = "ggplot2") +
  hd_geom_venn(sets = my_sets) +
  hd_opts(title = "Animals and Minerals")

# Extend a base list programmatically
base_sets <- list(
  hd_venn_set("A", "Oslo",   value = 120),
  hd_venn_set("B", "Bergen", value = 95)
)
extended <- c(base_sets, list(
  hd_venn_intersect(c("A", "B"), "Both", value = 40)
))
hd(backend = "highcharter") +
  hd_geom_venn(sets = extended) +
  hd_opts(title = "City overlap")

}
\seealso{
\code{\link[=hd_venn_set]{hd_venn_set()}}, \code{\link[=hd_venn_intersect]{hd_venn_intersect()}}
}
