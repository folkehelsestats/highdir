% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-map.R
\name{hd_geom_map}
\alias{hd_geom_map}
\title{Map (Choropleth) Layer for hd Objects}
\usage{
hd_geom_map(
  map_url,
  join_by = "hc-key",
  series_name = NULL,
  value_suffix = "",
  low_col = NULL,
  high_col = NULL,
  tooltip_fmt = NULL,
  nav_enabled = TRUE,
  ...
)
}
\arguments{
\item{map_url}{\strong{Required.} GeoJSON source in any of three forms:
\describe{
\item{URL}{\code{"https://..."} — fetched with \code{jsonlite::fromJSON()}.
Results are cached for the R session.  See \code{\link[=hd_map_no]{hd_map_no()}} for
Norway maps.}
\item{Local file path}{\code{"/path/to/file.geojson"} — read from disk
with \code{jsonlite::fromJSON()}.}
\item{Pre-parsed list}{A GeoJSON list already in memory, e.g. the
result of \code{jsonlite::fromJSON(url, simplifyVector = FALSE)}.
No download or disk read occurs — polygon filtering is applied
and the list is used directly.}
}}

\item{join_by}{Character. Property name in the GeoJSON features used
to join to \code{spec$x}.  Default \code{"hc-key"} (standard for Highcharts
map collections).}

\item{series_name}{Character. Series label in the legend / tooltip.
Defaults to \code{opts$ylab} or \code{spec$y}.}

\item{value_suffix}{Character. Suffix appended to values in tooltips and
legend labels.  E.g. \code{"\%"}.  Default \code{""}.}

\item{low_col}{Character. Hex colour for the low end of the gradient.
Default: \code{"#C6DBEF"}.}

\item{high_col}{Character. Hex colour for the high end of the gradient.
Default: \code{"#025169"}.}

\item{tooltip_fmt}{Character. Highcharts tooltip \code{pointFormat} string.
Default \code{"{point.name}: {point.value}"}.}

\item{nav_enabled}{Logical. Enable map pan/zoom controls.  Default \code{TRUE}.
Highcharter only.}

\item{...}{Additional arguments forwarded to \code{\link[=hd_make]{hd_make()}}.}
}
\value{
An S3 object of class \code{"hd_geom"} for use with \code{+.hd}.
}
\description{
Creates a choropleth map layer for use with \code{\link[=hd]{hd()}} and \code{+}.
Fetches GeoJSON from \code{map_url} and joins it to \code{spec$data} using the
column specified in \code{spec$x} (the join-key column) and colours regions
by \code{spec$y} (the value column).
}
\section{Color resolution}{

Two-color sequential gradient for the choropleth fill:
\enumerate{
\item Explicit \code{low_col} / \code{high_col} arguments
\item \code{opts$colors} as a 2-element vector (first = low, second = high)
\item Built-in highdir brand defaults: \code{"#C6DBEF"} (light) to
\code{"#025169"} (dark teal)
}
}

\section{Norway convenience}{

Use \code{\link[=hd_map_no]{hd_map_no()}} to get the standard URL for Norwegian municipality
or county maps from the Highcharts map collection without memorising
the raw GitHub URL.
}

\examples{
\donttest{
library(data.table)

# Norwegian municipality data
set.seed(42)
url <- hd_map_no("municipality")

# Fetch the keys from the GeoJSON so we can build a matching data frame
geojson  <- jsonlite::fromJSON(url, simplifyVector = FALSE)
features <- Filter(\(x) x$geometry$type \%in\%
  c("Polygon", "MultiPolygon"), geojson$features)

df <- data.frame(
  `hc-key` = sapply(features, \(x) x$properties$`hc-key`),
  name     = sapply(features, \(x) x$properties$name \%||\% NA_character_),
  value    = round(runif(length(features), 10, 100), 1),
  check.names = FALSE
)

# Dynamic mode
hd(df, x = "hc-key", y = "value", mode = "dynamic") +
  hd_geom_map(map_url = url) +
  hd_opts(title = "Norway", subtitle = "Random metric")

# Custom colours
hd(df, x = "hc-key", y = "value", mode = "dynamic") +
  hd_geom_map(map_url = url, low_col = "#FFEDA0", high_col = "#E31A1C") +
  hd_opts(title = "Custom palette")

# Static mode (requires sf)
hd(df, x = "hc-key", y = "value", mode = "static") +
  hd_geom_map(map_url = url) +
  hd_opts(title = "Static map")
}

}
\seealso{
\code{\link[=hd_map_no]{hd_map_no()}}, \code{\link[=hd]{hd()}}, \code{\link[=hd_spec]{hd_spec()}}, \code{\link[=hd_opts]{hd_opts()}}
}
