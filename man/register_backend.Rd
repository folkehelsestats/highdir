% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_backend}
\alias{register_backend}
\title{Register a Rendering Backend}
\usage{
register_backend(name, engine)
}
\arguments{
\item{name}{Character. Unique backend identifier (e.g. \code{"ggplot2"}).}

\item{engine}{A function with signature \verb{function(spec, geom, ...)}.}
}
\value{
\code{name}, invisibly.
}
\description{
Adds a named backend engine to the registry. An engine is a function with
the signature \verb{function(spec, geom, ...)} that returns a rendered figure
object (a \code{ggplot} or \code{highchart}).
}
\details{
Third-party packages can call \code{register_backend()} to add their own
backends (e.g. \code{"plotly"}).
}
\examples{
\dontrun{
my_engine <- function(spec, geom, ...) { ... }
register_backend("my_backend", my_engine)
}
}
