% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-ci.R
\name{create_ci_graph}
\alias{create_ci_graph}
\title{Create a Highchart with Confidence Intervals}
\usage{
create_ci_graph(
  data,
  x_col = "year",
  y_col = "adj_enhet",
  lower_col = "lower_enhet",
  upper_col = "upper_enhet",
  ylim = NULL,
  title = NULL,
  subtitle = NULL,
  y_axis_title = NULL,
  x_axis_title = NULL,
  series_name = "Antall enheter",
  line_color = "#206276",
  caption = NULL,
  credits_text = "Helsedirektoratet",
  credits_href = "https://www.helsedirektoratet.no/",
  save = FALSE
)
}
\arguments{
\item{data}{A data frame containing the data to plot.}

\item{x_col}{Character. Name of column in the dataset for x-axis. Default: \code{"year"}.}

\item{y_col}{Character. Name of column in the dataset for y-axis. Default: \code{"adj_enhet"}.}

\item{lower_col}{Character. Name of the lower CI column. Default: \code{"lower_enhet"}.}

\item{upper_col}{Character. Name of the upper CI column. Default: \code{"upper_enhet"}.}

\item{ylim}{Numeric vector of length 2, optional. Sets fixed y-axis limits as c(min, max).
If NULL (default), y-axis limits are determined automatically by the data.
Example: c(0, 70) sets y-axis from 0 to 70.}

\item{title}{Character. Main title for the chart.}

\item{subtitle}{Character. Subtitle text.}

\item{y_axis_title}{Character. Y-axis title.}

\item{x_axis_title}{Character. X-axis title.}

\item{series_name}{Character. Name for the main data series. Default: \code{"Antall enheter"}.}

\item{line_color}{Character. Hex color code for the line and area. Default: \code{"#206276"}.}

\item{caption}{Character. Chart caption text randered below the chart.}

\item{credits_text}{Character. Credits text. Default: \code{"Helsedirektoratet"}.}

\item{credits_href}{Character. URL for credits link. Default: Helsedirektoratet URL.}

\item{save}{Logical. Save file as a selfcontained HTML. Default: FALSE}
}
\value{
A \code{highchart} object that can be rendered or further customized.
}
\description{
This function creates an interactive highchart displaying data
over time with 95\% confidence intervals shown as a shaded area range.
}
\details{
The function creates a line chart with the following features:
\itemize{
\item Main line series showing the central values
\item Shaded area range showing 95\\% confidence intervals
\item Accessibility features enabled
\item Export functionality
\item Shared tooltip on hover
}
}
\examples{
\dontrun{
# Basic usage with default parameters
library(highcharter)
chart <- create_ci_graph(dtx)
chart

# Custom title and colors
chart <- create_ci_graph(
  data = dtx,
  title = "Custom Alcohol Use Chart",
  line_color = "#FF5733"
)

# Using different column names
chart <- create_ci_graph(
  data = my_data,
  x_col = "aar",
  y_col = "verdi",
  lower_col = "nedre_ci",
  upper_col = "ovre_ci"
)

# Full customization
chart <- create_ci_graph(
  data = dtx,
  title = "Alcohol Consumption Trends",
  subtitle = "Age and gender adjusted rates",
  y_axis_title = "Units of alcohol",
  x_axis_title = "Year",
  series_name = "Units",
  line_color = "#1E90FF",
  caption = "Source: Health Survey",
  credits_text = "Norwegian Directorate of Health"
)
}

}
\seealso{
\code{\link[highcharter]{highchart}}, \code{\link[highcharter]{hc_add_series}}
}
