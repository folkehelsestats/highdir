% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-venn.R
\name{hc_venn}
\alias{hc_venn}
\title{Highcharter Venn/Euler Geom Function}
\usage{
hc_venn(chart, spec, opts, geom_params, use_js = TRUE, ...)
}
\arguments{
\item{chart}{A \code{highchart} object (from base_fig() bypass path).}

\item{spec}{An \code{\link[=hd_spec]{hd_spec()}} object.  \verb{$data} is unused.}

\item{opts}{An \code{\link[=hd_opts]{hd_opts()}} object.}

\item{geom_params}{Named list.  Must contain \code{sets}; optionally
\code{series_name} and \code{label_font_size}.}

\item{use_js}{Logical. Unused for venn; present for engine consistency.}

\item{...}{Unused.}
}
\value{
The updated \code{highchart} object.
}
\description{
Adds a \code{"venn"} type series to a Highcharts chart.  Supports any number
of sets and any number of intersection entries.
}
\section{Highcharts venn series contract}{

Each entry in \code{sets} becomes one element of the Highcharts \code{data} array:
\itemize{
\item Single set: \code{list(sets = list("A"), name = "Animals", value = 5)}
\item Intersection: \code{list(sets = list("A", "B"), value = 2)}
}
\code{name} is optional for intersections but required for single sets if you
want a label in the diagram.
}

\keyword{internal}
