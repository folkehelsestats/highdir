% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hd-object.R
\name{print.hd}
\alias{print.hd}
\title{Render an hd Object}
\usage{
\method{print}{hd}(x, ...)
}
\arguments{
\item{x}{An \code{hd} object.}

\item{...}{Ignored; present for S3 consistency.}
}
\value{
The rendered output (a \code{highchart} widget or a \code{ggplot} object),
invisibly.
}
\description{
Printing an \code{\link[=hd]{hd()}} object triggers rendering.  The geometry type and any
geometry-specific parameters are extracted from the stored \code{hd_geom} layer;
presentation options from the \code{hd_opts} layer; the backend from the
\verb{$backend} slot.  All of these are forwarded to \code{\link[=hd_make]{hd_make()}}, which performs
the actual rendering via the registered engine.
}
\details{
You rarely need to call \code{print.hd()} directly — R calls it automatically
when the object appears at the top level, in knitr/Quarto chunks, or in
Shiny \code{renderHighchart()} / \code{renderPlot()} blocks.
}
