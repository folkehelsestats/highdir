% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-map.R
\name{.resolve_geojson}
\alias{.resolve_geojson}
\title{Resolve a GeoJSON source to a filtered list ready for rendering}
\usage{
.resolve_geojson(map_src)
}
\arguments{
\item{map_src}{A URL string, file path string, or parsed GeoJSON list.}
}
\value{
A list (parsed GeoJSON) containing only polygon features.
}
\description{
Accepts three forms of input and returns a consistently filtered GeoJSON
list - regardless of which form the user supplied:
}
\details{
\describe{
\item{Character string (URL)}{\code{"https://..."} - fetched with
\code{jsonlite::fromJSON()}.  Results are memoised for the R session so
repeated calls to the same URL do not re-download.}
\item{Character string (file path)}{\code{"/path/to/file.geojson"} -
read from disk with \code{jsonlite::fromJSON()}.  Same function, same
memoisation.}
\item{Pre-parsed list}{A GeoJSON list already in memory (e.g. the
output of \code{jsonlite::fromJSON(..., simplifyVector = FALSE)}).
Polygon filtering is applied but no download or disk read occurs.}
}
}
\keyword{internal}
