% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figure-spec.R
\name{hd_spec_venn}
\alias{hd_spec_venn}
\title{Create a Venn / Euler Figure Specification}
\usage{
hd_spec_venn(sets)
}
\arguments{
\item{sets}{A non-empty list of set entries built with \code{\link[=hd_venn_set]{hd_venn_set()}}
and \code{\link[=hd_venn_intersect]{hd_venn_intersect()}}, or raw named lists following the format:
\code{list(sets = list("A"), name = "Label", value = 5)}.}
}
\value{
An S3 object of class \code{c("hd_spec_venn", "hd_spec")}.
}
\description{
Declarative-API equivalent of \code{\link[=hd_spec]{hd_spec()}} for venn and euler diagrams.
Wraps a pre-built set list inside an \code{hd_spec} object so it can be passed
to \code{\link[=hd_make]{hd_make()}} using the same \code{spec / opts / hd_make()} pattern as every
other highdir geometry.
}
\section{Usage with hd_make()}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{sets <- list(
  hd_venn_set("A", "Oslo",   120),
  hd_venn_set("B", "Bergen",  95),
  hd_venn_intersect(c("A","B"), 40)
)

spec_v <- hd_spec_venn(sets)
opts_v <- hd_opts(title = "City overlap")

# Declarative API - identical pattern to every other geom
hd_make(spec_v, "venn", opts_v)                       # highcharter
hd_make(spec_v, "venn", opts_v, backend = "ggplot2")  # ggplot2
}\if{html}{\out{</div>}}
}

\section{Compared to the composable API}{

Both approaches produce identical output.  Use whichever fits your workflow:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Declarative (hd_spec / hd_opts / hd_make)
hd_make(hd_spec_venn(sets), "venn", hd_opts(title = "City overlap"))

# Composable (hd / + / hd_geom_venn)
hd(backend = "highcharter") +
  hd_geom_venn(sets = sets) +
  hd_opts(title = "City overlap")
}\if{html}{\out{</div>}}
}

\examples{
\donttest{
sets <- list(
  hd_venn_set("A", "Oslo",   value = 120),
  hd_venn_set("B", "Bergen", value = 95),
  hd_venn_intersect(c("A", "B"), value = 40)
)
spec_v <- hd_spec_venn(sets)
opts_v <- hd_opts(title = "City overlap")

hd_make(spec_v, "venn", opts_v)
hd_make(spec_v, "venn", opts_v, backend = "ggplot2")
hd_make(hd_spec_venn(sets), "venn", hd_opts(title = "City overlap"))
}

}
\seealso{
\code{\link[=hd_venn_set]{hd_venn_set()}}, \code{\link[=hd_venn_intersect]{hd_venn_intersect()}}, \code{\link[=hd_venn_sets_from_spec]{hd_venn_sets_from_spec()}},
\code{\link[=hd_make]{hd_make()}}, \code{\link[=hd_opts]{hd_opts()}}
}
