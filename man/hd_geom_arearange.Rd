% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hd-object.R
\name{hd_geom_arearange}
\alias{hd_geom_arearange}
\title{Add an Arearange (Confidence Band) Layer}
\usage{
hd_geom_arearange(ymin, ymax, ...)
}
\arguments{
\item{ymin}{Character. Column name for the lower bound of the range.}

\item{ymax}{Character. Column name for the upper bound of the range.}

\item{...}{Additional optional arguments forwarded to the geom function
(e.g. \code{show_line = FALSE}, \code{single_colour = "#025169"}).
See \link{geom_args("arearange")} for the full list.}
}
\value{
An S3 object of class \code{"hd_geom"} for use with \code{+.hd}.
}
\description{
Geometry layer for ribbon / confidence-interval charts.  Unlike the other
\verb{hd_geom_*()} functions, \code{ymin} and \code{ymax} are \strong{required} named arguments
(they map to column names in \code{spec$data}) rather than optional \code{...} extras.
This makes the contract explicit at the call site instead of burying
required information inside \code{...}.
}
\examples{
df <- data.frame(
  age  = c("18-24", "25-34", "35-44", "45-54"),
  pct  = c(42, 55, 48, 60),
  lo   = c(37, 50, 43, 55),
  hi   = c(47, 60, 53, 65)
)

hd(df, x = "age", y = "pct") +
  hd_geom_arearange(ymin = "lo", ymax = "hi") +
  hd_opts(title = "Estimate with 95\% CI", ylim = c(30, 70))

}
