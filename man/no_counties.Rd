% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-map.R
\name{no_counties}
\alias{no_counties}
\title{Norway County Reference Table}
\usage{
no_counties()
}
\value{
A \code{data.frame} with columns:
\describe{
\item{fylkesnummer}{Character. 2-digit SSB code, e.g. \code{"03"}.}
\item{name}{Character. Norwegian county name.}
\item{hc_key}{Character. Highcharts join key, e.g. \code{"no-03"}.}
}
}
\description{
Returns a data frame of the 15 Norwegian counties (fylker) with official
SSB \code{fylkesnummer}, Norwegian names, and the Highcharts \code{hc_key} values
used to join data to the \code{"countries/no/no-all"} map topology.
}
\details{
Use this to prepare your data for \code{hd_make(..., type = "map", level = "county")}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(dplyr)
my_data \%>\%
  left_join(no_counties(), by = "fylkesnummer") \%>\%
  hd_spec(x = "fylkesnummer", y = "rate") \%>\%
  hd_make("map", level = "county")
}\if{html}{\out{</div>}}
}
\examples{
no_counties()
}
