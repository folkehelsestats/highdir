# Convert fig_spec to a List

Convert fig_spec to a List

## Usage

``` r
# S3 method for class 'fig_spec'
as.list(x, ...)
```

## Arguments

- x:

  A `fig_spec` object.

- ...:

  Ignored.

## Value

A plain list (without the `data` slot, to keep output readable).
